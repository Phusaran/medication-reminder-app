# Medication Reminder System

ระบบ Medication Reminder เป็นแอปพลิเคชันสำหรับช่วยเตือนการรับประทานยา บันทึกประวัติการกินยา ติดตามอาการ และจัดการสต็อกยา โดยรองรับการดูแลผู้ป่วยผ่าน Caregiver

## ✨ Features

* 🔔 ตั้งเวลาเตือนการรับประทานยา (Schedule)
* 📋 บันทึกประวัติการกินยา (Taken / Skipped / Missed)
* 📦 จัดการสต็อกยา พร้อมแจ้งเตือนเมื่อยาใกล้หมด
* 👩‍⚕️ ระบบ Caregiver สำหรับผู้ดูแล
* 🧠 บันทึกอาการ (Symptom Log)
* 🗂 ฐานข้อมูลยา (Medication Master)

## 🗄 Database Structure

ฐานข้อมูลใช้ชื่อ **`medication_reminder`** (MySQL / MariaDB)

### ตารางหลัก

* `user` – ผู้ใช้งานระบบ (ผู้ป่วย)
* `caregiver` – ผู้ดูแล
* `caring` – ความสัมพันธ์ระหว่างผู้ป่วยและผู้ดูแล
* `admin` – ผู้ดูแลระบบ

### ระบบยา

* `medication_master` – ข้อมูลยากลาง
* `user_medication` – ยาที่ผู้ใช้เพิ่มเอง
* `schedule` – ตารางเวลาการกินยา
* `medication_log` – ประวัติการกินยา
* `stock` – สต็อกยา
* `symptom_log` – บันทึกอาการ

## 🧩 ER Diagram (Conceptual)

```
User ──< User_Medication ──< Schedule ──< Medication_Log
  │             │
  │             └── Stock
  │
  └──< Symptom_Log

User ──< Caring >── Caregiver
```

## ⚙️ Installation & Usage Guide (Step by Step)

คู่มือนี้อธิบายตั้งแต่ดาวน์โหลดโปรเจค → ตั้งค่าฐานข้อมูล → รันระบบจนใช้งานได้จริง

---

## 1️⃣ ดาวน์โหลดโปรเจค

1. ดาวน์โหลดไฟล์โปรเจค (.zip)
2. แตกไฟล์ไปยังโฟลเดอร์ที่ต้องการ เช่น

```bash
medication-reminder/
```

---

## 2️⃣ ติดตั้งโปรแกรมที่จำเป็น

### Software Requirements

* XAMPP / WAMP / Laragon

  * Apache
  * MySQL / MariaDB 10+
* (ถ้ามี Backend แยก)

  * Node.js 18+
  * npm / yarn

---

## 3️⃣ ตั้งค่าฐานข้อมูล

### 3.1 เปิด phpMyAdmin

* เข้าเว็บ `http://localhost/phpmyadmin`

### 3.2 สร้างฐานข้อมูล

```sql
CREATE DATABASE medication_reminder CHARACTER SET utf8mb4;
```

### 3.3 Import Database

1. เลือกฐานข้อมูล `medication_reminder`
2. ไปที่เมนู **Import**
3. เลือกไฟล์ `medication_reminder.sql`
4. กด **Go**

✔ เมื่อเสร็จแล้วจะเห็นตารางทั้งหมด เช่น `user`, `schedule`, `medication_log`

---

## 4️⃣ ตั้งค่า Backend (ตัวอย่าง Node.js)

### 4.1 ติดตั้ง Dependencies

```bash
npm install
```

### 4.2 ตั้งค่าไฟล์ `.env`

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=medication_reminder
```

### 4.3 รันเซิร์ฟเวอร์

```bash
npm start
```

Server จะรันที่

```bash
http://localhost:3000
```

---

## 5️⃣ ตั้งค่า Frontend / Mobile App (Expo Go)

ระบบ Frontend เป็น **React Native (Expo)** และทดสอบผ่าน **Expo Go**

---

### 5.1 ติดตั้งเครื่องมือที่จำเป็น

ติดตั้ง Node.js (ถ้ายังไม่มี)

* แนะนำ Node.js 18+

ติดตั้ง Expo CLI

```bash
npm install -g expo-cli
```

ติดตั้งแอป **Expo Go** บนมือถือ

* Android: Play Store
* iOS: App Store

---

### 5.2 ติดตั้ง Dependencies

เข้าโฟลเดอร์ Frontend

```bash
cd frontend
npm install
```

---

### 5.3 ตั้งค่า API URL

แก้ไขไฟล์ config หรือไฟล์ที่เรียก API เช่น

```js
const API_URL = "http://<IP-COMPUTER>:3000";
```

⚠️ ห้ามใช้ `localhost` ให้ใช้ IP เครื่อง เช่น `192.168.1.10`

---

### 5.4 รันแอปด้วย Expo

```bash
npx expo start
```

เมื่อรันแล้วจะเห็น QR Code ใน Terminal หรือ Browser

---

### 5.5 เปิดแอปบนมือถือ

1. เปิดแอป **Expo Go**
2. สแกน QR Code
3. แอปจะรันทันทีบนมือถือ

✔ มือถือและคอมต้องอยู่ Wi‑Fi เดียวกัน

---

### 5.6 การทดสอบ

* Login / Register
* เพิ่มยา
* ตั้งเวลาเตือน
* รับ Notification

---

## 6️⃣ การเข้าใช้งานระบบ

### 6.1 Login

* ผู้ใช้ Login ด้วย Email + Password
* Admin ใช้ Username + Password

### 6.2 เพิ่มยา

1. เลือก **เพิ่มยาใหม่**
2. กำหนดชื่อยา / ขนาด / วันเริ่ม-สิ้นสุด
3. ตั้งเวลาเตือน

### 6.3 เมื่อถึงเวลา

* ระบบแจ้งเตือน
* ผู้ใช้เลือก **กินแล้ว / ข้าม**
* ระบบบันทึกลง `medication_log`

### 6.4 ติดตามสต็อกยา

* จำนวนยาจะลดอัตโนมัติ
* แจ้งเตือนเมื่อยาใกล้หมด

---

## 7️⃣ Caregiver System

1. ผู้ป่วยส่ง Invite Code
2. Caregiver กดยอมรับ
3. ดูข้อมูลการกินยาและอาการของผู้ป่วยได้

---

## 8️⃣ Troubleshooting

* ❌ ต่อ DB ไม่ได้ → ตรวจ `.env`
* ❌ ภาษาเพี้ยน → ตรวจ charset เป็น `utf8mb4`
* ❌ ไม่แจ้งเตือน → ตรวจ device_token

---

## 🔐 Default Accounts

### Admin

* Username: `admin`
* Password: 123456

### Test User

* Email: `test@gmail.com`

> Password: 123456

### Caregiver

* Email: `mom@email.com`

> Password: 123456

## 📌 Notes

* ระบบรองรับ Device Token สำหรับ Push Notification
* สามารถขยายเป็น Firebase / FCM ได้
* โครงสร้างรองรับ Mobile Application


---

พัฒนาเพื่อการศึกษาและใช้งานด้านสุขภาพ 💊
